import { emptyState } from '../components/loading-state.js';

export async function renderTransfers(){
  return emptyState('Transfers kommer i Fas 2', 'Transfers ska flyttas till samma adminuppdaterade snapshotmodell som statistik.');
}
