import { emptyState } from '../components/loading-state.js';

export async function renderTeam(){
  return emptyState('Lagsida kommer i Fas 2', 'Nästa steg är att koppla lagsidor till samma publika statistik-cache.');
}
