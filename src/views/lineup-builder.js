import { emptyState } from '../components/loading-state.js';

export async function renderLineupBuilder(){
  return emptyState('Lineup Builder kommer i Fas 2', 'Den nya buildern flyttas till egen modul så export, pitch och UI kan hållas isolerat.');
}
