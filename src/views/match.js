import { emptyState } from '../components/loading-state.js';

export async function renderMatch(){
  return emptyState('Matchvy kommer i Fas 2', 'Grunden är förberedd, men matchdetaljer flyttas över efter de första publika vyerna.');
}
