import { emptyState } from '../components/loading-state.js';

export async function renderAdmin(){
  return emptyState('Admin UI kommer i Fas 2', 'Backend-actions finns kvar i /api/admin. Ny adminpanel byggs ovanpå samma actionmodell.');
}
